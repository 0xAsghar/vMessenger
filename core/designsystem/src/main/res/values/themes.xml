<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        The window a modal sheet opens in: the whole screen, transparent, behind neither system bar.
        The sheet draws its own scrim and its own motion, so the platform's dim and window
        animation are both off; one of each is enough.
    -->
    <style name="VmModalWindow" parent="android:Theme.Dialog">
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowClipToOutline">false</item>
    </style>
</resources>
