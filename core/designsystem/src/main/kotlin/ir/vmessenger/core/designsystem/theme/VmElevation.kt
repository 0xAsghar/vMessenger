package ir.vmessenger.core.designsystem.theme

import androidx.compose.ui.unit.dp

/** Shadow elevations; tonal elevation is handled by the `surfaceContainer*` roles instead. */
object VmElevation {
    val none = 0.dp
    val bar = 1.dp
    val sheet = 3.dp
    val fab = 6.dp
}
